# AGENTS.md

이 파일은 이 프로젝트에서 Codex가 작업할 때 따라야 할 개발 지침입니다. 서비스 기획서는 별도로 제공된다고 가정하며, 구현 전에는 항상 기획 의도, 사용자 흐름, 데이터 모델, 배포 환경을 먼저 확인합니다.

## 프로젝트 목표

- Vercel 배포를 전제로 한 웹 서비스 개발
- 프론트엔드와 백엔드를 하나의 프로젝트 안에서 함께 관리
- 서비스 기획을 실제 동작하는 프로덕트로 구현
- 초기 구현은 빠르게 검증 가능하게 만들고, 이후 확장 가능한 구조로 정리

## 기본 기술 방향

- 이 프로젝트는 과제 제약에 따라 순수 HTML, CSS, JavaScript로 프론트엔드를 구현합니다.
- React, Vue, Next.js 같은 프론트엔드 프레임워크는 사용하지 않습니다.
- 백엔드는 Vercel Serverless Functions의 Python 엔드포인트를 `api/` 폴더에 구현합니다.
- 프론트엔드는 `fetch('/api/...')`로 Python 백엔드를 호출합니다.
- 배포 대상은 Vercel입니다.
- 데이터베이스, 인증, 스토리지, 결제, 이메일 등 외부 서비스는 기획 요구와 운영 비용을 비교해 선택합니다.
- 환경 변수는 `.env.local`에서 관리하고, 실제 값은 커밋하지 않습니다.
- Python 패키지는 `requirements.txt`에 정의합니다.

## 작업 전 확인할 것

서비스 기획이 제공되면 먼저 아래 항목을 정리합니다.

- 핵심 사용자와 사용 목적
- 주요 화면 목록
- 사용자 흐름
- 필요한 데이터 모델
- 권한과 인증 범위
- 관리자 기능 필요 여부
- 외부 API 또는 SaaS 연동 여부
- MVP에 반드시 필요한 기능과 나중에 미뤄도 되는 기능

기획이 모호한 경우에는 임의 구현을 넓게 하지 말고, 영향이 큰 의사결정만 짧게 질문합니다. 나머지는 합리적인 기본값으로 진행합니다.

## SDD 개발 원칙

이 프로젝트는 SDD(Spec-Driven Development, 스펙 주도 개발)를 기본 개발 방식으로 사용합니다. 구현은 명세를 따른 결과물이어야 하며, 기능이 커질수록 코드보다 먼저 명세를 갱신합니다.

### 명세 우선순위

서비스 기획을 받으면 바로 구현하지 않고, 먼저 다음 문서를 작성하거나 갱신합니다.

```text
docs/
  product-spec.md
  user-flows.md
  data-model.md
  api-spec.md
  implementation-plan.md
  assignment-criteria.md
```

- `docs/product-spec.md`: 서비스 목적, 사용자, 핵심 가치, MVP 범위
- `docs/user-flows.md`: 주요 사용자 흐름, 화면 전환, 예외 흐름
- `docs/data-model.md`: 엔티티, 필드, 관계, 권한 기준
- `docs/api-spec.md`: Python API의 입력, 출력, 에러, 권한
- `docs/implementation-plan.md`: 구현 순서, 의존성, 미해결 결정사항
- `docs/assignment-criteria.md`: 과제 제출물, 평가 기준, 제약 조건, 제출 전 체크리스트

프로젝트 규모가 작을 때는 하나의 `docs/spec.md`로 시작할 수 있습니다. 기능이 늘어나면 위 문서들로 분리합니다.

### 구현 전 필수 확인

코드 변경 전에 해당 기능의 명세에서 아래가 확인되어야 합니다.

- 사용자 관점의 완료 조건
- 정상 흐름과 실패 흐름
- 필요한 데이터와 저장 위치
- 인증과 권한 규칙
- 프론트엔드 화면, 섹션, JavaScript 동작 범위
- Python API와 외부 연동 범위
- Vercel 배포 환경에서 필요한 환경 변수
- 과제 평가 기준 충족 여부

명세가 없거나 모호하면 먼저 명세 초안을 작성하고, 영향이 큰 선택지만 사용자에게 확인합니다.

### 명세 변경 규칙

- 구현 중 기획과 다른 판단이 필요하면 코드보다 명세를 먼저 수정합니다.
- API 응답, DB 스키마, 권한 규칙처럼 다른 코드에 영향을 주는 변경은 반드시 명세에 반영합니다.
- 명세와 코드가 충돌하면 명세를 기준으로 판단하되, 명세가 현실과 맞지 않으면 명세 수정안을 먼저 제안합니다.
- 단순 스타일, 문구, 작은 UI 배치처럼 서비스 동작에 영향이 적은 변경은 명세 갱신 없이 진행할 수 있습니다.

### SDD 작업 흐름

1. 서비스 기획을 읽고 요구사항을 기능 단위로 나눕니다.
2. MVP 범위와 후순위 범위를 구분합니다.
3. 명세 문서를 작성하거나 갱신합니다.
4. 화면, 데이터 모델, API 계약을 먼저 확정합니다.
5. 명세 기준으로 구현 계획을 세웁니다.
6. 구현 후 명세의 완료 조건을 기준으로 검증합니다.
7. 구현 중 바뀐 결정사항을 명세에 다시 반영합니다.

## 권장 프로젝트 구조

이 과제 프로젝트는 다음 구조를 우선합니다.

```text
index.html
css/
js/
api/
images/
data/
docs/
requirements.txt
README.md
```

- `index.html`: 서비스의 단일 HTML 진입점
- `css/`: 스타일시트
- `js/`: 화면 동작, 입력 검증, API 호출
- `api/`: Vercel Python Serverless Functions
- `images/`: 정적 이미지와 스크린샷 후보 에셋
- `data/`: 정적 문제 JSON
- `docs/`: SDD 명세 문서
- `requirements.txt`: Python 백엔드 의존성
- `README.md`: 제출용 프로젝트 설명

프로젝트가 커져도 과제 제약을 우선하며, 프레임워크 도입은 하지 않습니다.

## 프론트엔드 원칙

- 첫 화면은 실제 서비스 사용 경험으로 시작합니다. 단순 소개용 랜딩 페이지는 기획상 필요할 때만 만듭니다.
- 화면은 사용자의 주요 업무 흐름을 기준으로 설계합니다.
- HTML, CSS, JavaScript 파일의 역할을 명확히 나눕니다.
- JavaScript는 사용자 입력 검증, 문제 전환, `fetch` 요청, 결과 렌더링을 담당합니다.
- 모바일과 데스크톱 모두에서 텍스트 겹침, 버튼 넘침, 레이아웃 깨짐이 없도록 확인합니다.
- 접근성 기본값을 지킵니다: 명확한 버튼 텍스트, label, focus 상태, 키보드 조작 가능성.
- 아이콘이 필요한 UI에는 프로젝트에 아이콘 라이브러리가 있으면 그것을 우선 사용합니다.

## 백엔드 원칙

- API는 프론트엔드 요구가 아니라 도메인 동작 기준으로 설계합니다.
- API는 `api/` 폴더의 Python 파일로 구현합니다.
- 입력값 검증은 서버에서 반드시 수행합니다.
- 인증이 필요한 API는 서버에서 권한을 검증합니다.
- 외부 API 키, DB URL, 토큰 등 비밀 값은 환경 변수로만 접근합니다.
- Vercel 서버리스 환경을 고려해 장시간 실행 작업, 로컬 파일 저장, 메모리 상태 의존을 피합니다.
- 파일 업로드가 필요하면 Vercel Blob, S3, Supabase Storage 등 외부 스토리지를 검토합니다.

## 데이터베이스와 상태

- 데이터 모델은 먼저 서비스 개념에 맞게 설계하고, 화면 편의를 위해 억지로 왜곡하지 않습니다.
- MVP에서는 단순한 스키마를 우선하되, 사용자, 권한, 결제, 감사 로그처럼 나중에 바꾸기 어려운 영역은 초기에 신중히 잡습니다.
- 클라이언트 상태는 UI 상호작용에 한정하고, 영속 데이터는 서버와 DB를 기준으로 관리합니다.

## Vercel 배포 기준

- 빌드 명령, 환경 변수, 런타임 제약을 Vercel 기준으로 확인합니다.
- 배포 전 최소한 다음을 검증합니다.
  - 정적 페이지가 정상 렌더링되는지 확인
  - Python API가 Vercel Serverless Functions 구조에 맞는지 확인
  - `requirements.txt`에 필요한 패키지가 정의되어 있는지 확인
  - 핵심 사용자 흐름 수동 확인
- Vercel 배포에서 필요한 환경 변수 목록은 문서화합니다.
- Preview 배포와 Production 배포의 차이를 고려해 외부 서비스 webhook URL, callback URL을 구분합니다.

## 과제 평가 기준

- 과제 제출 기준은 `docs/assignment-criteria.md`에 정리합니다.
- 기능 구현 전에는 SDD 명세와 과제 평가 기준을 함께 확인합니다.
- SDD 명세와 과제 평가 기준이 충돌하면 과제 평가 기준을 우선하되, 사용자에게 변경 내용을 알립니다.
- 제출 전에는 `docs/assignment-criteria.md`의 제출 전 체크리스트를 기준으로 최종 검증합니다.
- 제출 자료나 스크린샷에 API 키가 노출되지 않도록 확인합니다.

## Skill 활용 기준

이 프로젝트에서는 필요한 경우 Codex Skill을 사용해 배포와 AI 관측 작업을 진행합니다.

### Vercel Skill

- Vercel 배포 요청이 있으면 `vercel-deploy` Skill을 사용합니다.
- 사용자가 명시적으로 Production 배포를 요청하지 않으면 Preview 배포를 기본으로 합니다.
- 배포 전에는 Vercel CLI 설치 여부를 먼저 확인합니다.
- 배포 명령이 네트워크 제한으로 실패하면 사용자 승인을 받아 다시 실행합니다.
- 배포가 완료되면 Preview URL을 사용자에게 전달합니다.
- fallback 배포를 사용한 경우 claim URL도 함께 전달합니다.

### Langfuse Skill

- AI 평가 품질 관측, trace 확인, 프롬프트 관리, dataset, score, session 확인이 필요하면 `langfuse` Skill을 사용합니다.
- Langfuse 관련 구현 전에는 최신 공식 문서를 먼저 확인합니다.
- Langfuse SDK와 API는 특별한 이유가 없으면 최신 버전을 기준으로 사용합니다.
- API 키는 채팅에 노출하지 않고 환경 변수로만 관리합니다.
- 예상 환경 변수는 `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_SECRET_KEY`, `LANGFUSE_HOST`입니다.
- `/api` 같은 AI 평가 흐름은 향후 Langfuse trace 대상으로 우선 고려합니다.
- 프롬프트를 Langfuse로 이전하거나 관리하는 경우, 코드에 흩어진 프롬프트와 Langfuse의 prompt 버전이 충돌하지 않도록 명세를 갱신합니다.

## 코드 변경 규칙

- 기존 파일과 사용자 변경사항을 함부로 되돌리지 않습니다.
- 변경 범위는 요청된 기능과 직접 관련된 부분으로 제한합니다.
- 새 의존성은 필요성이 명확할 때만 추가합니다.
- 반복 코드가 실제로 문제를 만들 때만 추상화합니다.
- 중요한 동작 변경에는 명세 갱신과 검증 절차를 함께 추가합니다.

## Codex 응답 방식

- 구현 전에는 먼저 현재 프로젝트 구조를 확인합니다.
- 기능 구현 전에는 관련 명세가 있는지 확인하고, 없으면 명세 초안을 먼저 작성합니다.
- 과제 제출과 관련된 작업에서는 `docs/assignment-criteria.md`를 함께 확인합니다.
- 작업 중에는 어떤 파일을 왜 수정하는지 간단히 알립니다.
- 완료 후에는 변경 파일, 핵심 변경 내용, 검증 결과를 짧게 보고합니다.
- 검증을 실행하지 못했거나 외부 설정이 필요하면 명확히 말합니다.

## 서비스 기획서를 받았을 때의 진행 순서

1. 기획 내용을 기능 단위로 분해합니다.
2. MVP 범위와 후순위 범위를 구분합니다.
3. SDD 명세 문서를 작성하거나 갱신합니다.
4. 과제 평가 기준과 충돌하는 부분이 없는지 확인합니다.
5. 화면 구조, 데이터 모델, API 계약을 먼저 제안합니다.
6. 필요한 기술 스택과 외부 서비스를 결정합니다.
7. 프로젝트 스캐폴딩 또는 기존 코드 수정을 진행합니다.
8. 명세의 완료 조건과 과제 체크리스트를 기준으로 로컬 실행과 빌드를 검증합니다.
9. Vercel 배포에 필요한 설정을 정리합니다.
